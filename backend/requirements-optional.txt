# Optional / extras
# Install these only if you want transactional email support via Resend
resend>=0.6.0
# Optional ML/face recognition (native build may fail on some hosts)
face-recognition>=1.3.0
git+https://github.com/ageitgey/face_recognition_models
# face_recognition_models still imports pkg_resources on supported Python versions.
setuptools<81
